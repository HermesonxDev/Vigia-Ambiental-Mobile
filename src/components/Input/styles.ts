import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: '100%',
        height: 52,
        borderRadius: 10,
        borderWidth: 1,
        borderColor: "#121214",
        backgroundColor: 'white',
        padding: 12,
        fontSize: 16
    }
})