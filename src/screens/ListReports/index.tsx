import { Text, View } from "react-native"
import { styles } from "./styles"

const ListReports: React.FC = () => {
    return (
        <View style={styles.container}>
            <Text style={styles.title}>List Reports</Text>
        </View>
    )
}

export default ListReports